# These are the project files for the project and section they are featured in

# Install the dependencies for feathers in root folder

npm install

# Start the application

npm start

#Install the dependencies for Angular in the "/public" folder

npm install

# run app
npm start

